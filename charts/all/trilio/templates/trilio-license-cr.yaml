{{- $license := (lookup "v1" "ConfigMap" "trilio-system" "trilio-license") }}
{{- if $license }}
apiVersion: triliovault.trilio.io/v1
kind: License
metadata:
  name: trilio-license
  namespace: trilio-system
spec:
  key: {{ $license.data.key | quote }}
{{- end }}
